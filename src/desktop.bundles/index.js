import 'jquery-validation'

import { initFormNumberSelection } from '../common.blocks/form-number-selection/form-number-selection.js';

import './index.scss';

window.addEventListener('load', function() {
    initFormNumberSelection();
})